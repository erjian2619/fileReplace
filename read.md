# FileReplaceTool

轻量文件替换工具，用于在 Windows 环境中快速把源文件或整个目录内容同步到目标目录，并在冲突时保持安全处理。

## 功能说明

- 支持两种替换模式：
  - 文件替换：可选择单个文件或多个文件，复制到目标目录根部
  - 整个目录替换：复制整个源目录到目标位置，并可指定新目录名称
- 支持拖放文件或目录到界面中直接处理
- 支持浏览器选择文件与目录
- 支持保存最近使用的目标目录，方便下次快速操作
- 在目录重复时会保留旧目录，避免直接覆盖导致数据丢失
- 可选择“游戏预设”目标目录，适合整理游戏资源文件和补丁替换

## 使用方式

1. 运行 `FileReplaceTool.exe`，或者执行 `build.bat` 编译程序。
2. 选择替换模式：
   - 文件替换：选择源文件，指定目标目录
   - 整个目录替换：选择源目录，填写新目录名，指定目标目录
3. 点击“检查并开始替换”执行操作。
4. 程序会在替换前检查目录和文件冲突，并给出提示。

## 目录结构

- `FileReplaceTool.cs`：主程序入口与 WinForms 逻辑
- `ModernMainForm.cs`：现代化界面与交互实现
- `FileReplaceTool.manifest`：程序清单配置
- `build.bat`：Windows 编译脚本
- `README.md`：项目基础说明
- `read.md`：补充说明文档

## 编译说明

该项目基于 .NET Framework / C# WinForms，适用于 Windows 7 及以上版本。

在 Windows 环境中运行：

```bat
build.bat
```

如果环境中已安装 .NET Framework 编译器，脚本会生成 `FileReplaceTool.exe`。

## 说明

该工具适合以下场景：

- 批量更新资源文件
- 替换游戏目录中的指定文件
- 复制目录结构并保留旧版本
- 快速整理文件内容而不破坏现有目录结构

> 注意：执行替换前请确认目标目录正确，尤其是在覆盖文件或重命名目录时。 
